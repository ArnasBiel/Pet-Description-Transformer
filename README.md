# Pet-Description-Transformer
A Transformer-based language model trained to generate pet adoption descriptions, built on data from PetFinder. The project also includes KMeans clustering on linguistic features to analyse and potentially enhance model training
